import { z } from "zod";

const text = (max: number) => z.string().trim().min(1, "Champ requis.").max(max);
const optionalText = (max: number) => z.string().trim().max(max).optional().transform((value) => value || null);
const optionalNumber = (max: number) => z.number().int().min(0).max(max).nullable();
const imageSchema = z.object({ url: z.string().trim().refine((value) => /^\/(?!\/)/.test(value) || /^https:\/\//i.test(value), "URL d’image non sécurisée."), altFr: optionalText(180), altAr: optionalText(180), isMain: z.boolean(), displayOrder: z.number().int().min(0).max(99) });
export const vehicleInputSchema = z.object({ brand: text(80), model: text(80), slug: z.string().trim().toLowerCase().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/, "Slug URL invalide.").max(120), year: z.number().int().min(1900).max(new Date().getFullYear() + 1).nullable(), categoryId: z.string().cuid(), fuel: z.enum(["DIESEL", "GASOLINE", "HYBRID", "ELECTRIC"]).default("DIESEL"), transmission: z.enum(["MANUAL", "AUTOMATIC"]).default("MANUAL"), seats: z.number().int().min(1).max(20), doors: z.number().int().min(1).max(10), luggage: optionalNumber(20), airConditioning: z.boolean().default(false), shortDescriptionFr: optionalText(300), fullDescriptionFr: optionalText(4000), badgeFr: optionalText(80), priceNoteFr: optionalText(160), shortDescriptionAr: optionalText(300), fullDescriptionAr: optionalText(4000), badgeAr: optionalText(80), priceNoteAr: optionalText(160), colorFr: optionalText(60), colorAr: optionalText(60), availability: z.enum(["AVAILABLE", "UNAVAILABLE", "MAINTENANCE", "RESERVED"]).default("AVAILABLE"), active: z.boolean().default(true), featured: z.boolean().default(false), displayOrder: z.number().int().min(0).max(100000).default(0), images: z.array(imageSchema).min(1, "Ajoutez au moins une image.").max(12).superRefine((images, ctx) => { const urls = new Set<string>(); images.forEach((image, index) => { if (urls.has(image.url)) ctx.addIssue({ code: "custom", path: [index, "url"], message: "Cette URL est déjà utilisée." }); urls.add(image.url); }); }), });
export type VehicleInput = z.infer<typeof vehicleInputSchema>;
