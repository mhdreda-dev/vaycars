import { Navigation } from "@/components/navigation"; import { SiteFooter } from "@/components/site-footer"; import { getAgencySettings } from "@/lib/getAgencySettings";
export default async function About(){const settings=await getAgencySettings();return <><Navigation/><main className="mx-auto max-w-3xl px-4 py-20 sm:px-6"><p className="text-sm font-bold uppercase tracking-[.16em] text-[#0b5aa7]">{settings.agencyName}</p><h1 className="mt-2 text-4xl font-black text-[#10233c]">Une location locale, simple et fiable.</h1><p className="mt-7 text-lg leading-8 text-slate-600">Basée à {settings.city}, {settings.agencyName} propose des véhicules propres et entretenus pour accompagner vos déplacements au {settings.country}. Notre équipe privilégie une communication claire et un service de proximité.</p></main><SiteFooter/></>}
