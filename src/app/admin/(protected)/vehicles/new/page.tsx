import { VehicleForm, emptyVehicle } from "@/components/admin/vehicle-form";
import { getAdminVehicleFilterOptions, getNextAdminDisplayOrder } from "@/lib/admin/vehicles";
export default async function NewVehiclePage() { const [categories, displayOrder] = await Promise.all([getAdminVehicleFilterOptions(), getNextAdminDisplayOrder()]); return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><p className="text-sm font-bold uppercase tracking-[.16em] text-[#0b5aa7]">Véhicules</p><h1 className="mt-2 text-3xl font-black text-[#10233c]">Ajouter une voiture</h1><p className="mt-3 text-sm text-slate-600">Ajoutez les informations françaises et arabes, puis importez des images ou conservez des URL existantes.</p><div className="mt-7"><VehicleForm categories={categories} initial={{ ...emptyVehicle, displayOrder: String(displayOrder) }} /></div></main>; }
