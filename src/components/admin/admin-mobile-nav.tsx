import Link from "next/link";
import { CarFront, LayoutDashboard, ListTree, Settings } from "lucide-react";
export function AdminMobileNav() { return <nav aria-label="Navigation administration" className="flex gap-2 overflow-x-auto border-b border-slate-200 bg-white px-4 py-3 lg:hidden">{[["/admin", "Tableau", LayoutDashboard], ["/admin/vehicles", "Véhicules", CarFront], ["/admin/categories", "Catégories", ListTree], ["/admin/settings", "Paramètres", Settings]].map(([href, label, Icon]) => { const I = Icon as typeof LayoutDashboard; return <Link key={href as string} href={href as string} className="flex min-h-11 shrink-0 items-center gap-2 rounded-xl border border-slate-200 px-3 text-sm font-bold text-[#10233c]"><I className="size-4" />{label as string}</Link>; })}</nav>; }
