"use client";
import { ChevronDown } from "lucide-react";
import { AnimatePresence, motion, useReducedMotion } from "framer-motion";
import { useId, useState } from "react";
export function AdminFormSection({ title, summary, children, defaultOpen = true, forceOpen = false }: { title: string; summary: string; children: React.ReactNode; defaultOpen?: boolean; forceOpen?: boolean }) { const [open,setOpen]=useState(defaultOpen); const id=useId(); const reduced=useReducedMotion(); const expanded=open||forceOpen; return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><button type="button" onClick={()=>setOpen(v=>!v)} aria-expanded={expanded} aria-controls={id} className="flex min-h-16 w-full items-center justify-between gap-4 p-5 text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-[#0b5aa7]"><span><span className="block text-lg font-black text-[#10233c]">{title}</span><span className="mt-1 block text-sm font-normal text-slate-500">{summary}</span></span><ChevronDown className={`size-5 shrink-0 text-[#0b5aa7] transition-transform ${expanded?"rotate-180":""}`}/></button><AnimatePresence initial={false}>{expanded&&<motion.div id={id} initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} transition={{duration:reduced?0.01:0.2}} className="overflow-hidden"><div className="border-t border-slate-100 p-5">{children}</div></motion.div>}</AnimatePresence></section> }
